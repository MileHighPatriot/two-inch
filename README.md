# Two-Inch draft cockpit

Maren's Sep 6, 2026 board for The Two-Inch Invitational (All Gut No Shaft). ESPN 10-team full PPR, 1QB, Robust-RB.

```bash
npm install
npm test
npm run dev
```

Source notes live in `content/`. Structured data in `src/data/`. Draft rules in `src/engine/`.
